{
  "name": "tie-dye-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "gallery:dev": "wrangler dev --config worker/wrangler.jsonc --port 8787",
    "gallery:deploy": "wrangler deploy --config worker/wrangler.jsonc",
    "gallery:types": "wrangler types --config worker/wrangler.jsonc worker/env.d.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^6.4.1",
    "wrangler": "^4.139.0"
  },
  "engines": {
    "node": ">=22"
  }
}
